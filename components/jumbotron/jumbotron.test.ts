import Jumbotron from './Jumbotron';

describe('the bumbotron component that is rendered at the top of the home page. this should display the background of the movie', () => {
	it('renders a play button', () => {
		expect(typeof Jumbotron).toBe(Function);
	});
});
