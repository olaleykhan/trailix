import React from 'react';
import styled from 'styled-components';

const Navbar = () => {
	return <Nav>Netflix</Nav>;
};

const Nav = styled.nav`
	height: 3rem;
	line-height: 3rem;
`;

export default Navbar;
